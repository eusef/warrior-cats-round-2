{
  "name": "warrior-cats-signal",
  "private": true,
  "version": "0.1.0",
  "description": "WebRTC signalling relay for Warrior Cats two-player co-op. Introduces two iPads, then gets out of the way.",
  "scripts": {
    "//": "Plain http on LOOPBACK ONLY. Vite proxies /signal to it, so the relay is never exposed on the network and never needs a certificate. Do not add --ip 0.0.0.0.",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
